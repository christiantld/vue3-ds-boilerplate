{
  "name": "design-system",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "generate:component:vue": "yarn workspace @mesa/generators generate"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  },
  "workspaces": [
    "actions/**",
    "config/**",
    "packages/**"
  ],
  "eslintConfig": {
    "extends": [
      "@mesa/eslint-config"
    ]
  },
  "prettier": "@mesa/prettier-config",
  "license": "MIT"
}
