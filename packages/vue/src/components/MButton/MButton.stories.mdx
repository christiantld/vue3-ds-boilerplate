<!-- Button.stories.mdx-->

import { Meta, Story } from '@storybook/addon-docs'

import MButton from './MButton.vue'

<Meta title="MButton" component={MButton} />

# Button

export const Template = (args) => ({
  components: { MButton },
  setup() {
    return { args }
  },
  template: `
    <MButton v-bind="args">`,
})

<Story
  name="Primary"
  argTypes={{
    variant: {
      options: ['primary', 'secondary', 'tertiary', 'default'],
      control: { type: 'radio' },
    },
    size: {
      options: ['sm', 'md', 'lg', 'full'],
      control: { type: 'select' },
    },
    disabled: {
      control: { type: 'boolean' },
    },
  }}
  args={{
    label: 'Button',
    variant: 'primary',
    disabled: false,
    size: 'md',
  }}
>
  {Template.bind({})}
</Story>
